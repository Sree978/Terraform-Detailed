To connect server over VS code
Install extenstion > Remote -ssh
Open a remote window > connect to host + enter and open  config file  (  c/users/username/.ssh/config)

Host terraform-server
  HostName 107.22.155.244
  User ec2-user
  IdentityFile C:\Users\Lokeswari\Downloads\awshome.pem

Open a remote window > connect to ssh > terraform server ( which we created above)
open a terminal and give permission to root user 
sudo chmod 777 /root

Provider.tf 
provider "aws" {
    region = "us-east-1"
}

Main.tf 
resource "aws_instance" "myinstance" {
    tags = {
        name = "myserver"              /   Name = ${terraform.workspace}-server   ---> to get dafault workspace user name as a server name 
    }
    ami = "ami-0521cb2d60cfbb1a6"
    instanc_type = "t3.micro"
    key_name = "awshome"
    root_block_device {
        volume_size = 10
    }
}

Terraform init
terraform plan    --> we have to add IAM role for our ec2 server 
terraform apply --auto-approve

  ------> workspace <------

In Terraform, a workspace allows you to manage multiple environments (such as dev, test, and prod) using the same Terraform configuration.

Terraform workspace show  --> will default workspace  --> 
Terraform workspace list  --> to get list of workspce

Terraform workspace new sree   --> 
Terraform workspace show   --> to check present workspace
 terraform apply --auto-approve --> will notice created one more server 

Local.tf 
locals {
  instance_type = {
    dev = "t3.micro"
    test = "c7i-flex.large"
    prod = "m7i-flex.large"
  }
}

main.tf 

will change only 

 instance_type = local.instance_type[terraform workspace]

Based on this instance type will craete  : instance type will pick up from locals.tf  its proceed accordings 
in dev workspace its create t3 micro , if its in test workapsce its create c7i


